/**
 * Deterministic sample data for the renderer lives in @mad/export so the
 * studio canvas and the deployed static page draw identical rows, threads and
 * series for the same node id.
 */
export { cellFor, chatThread, hashSeed, initials, personName, rng, series, taskCard } from '@mad/export';
