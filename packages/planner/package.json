{
  "name": "@mad/planner",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic prompt-to-document planner for MAD Studio. Turns a natural-language prompt into a timed stream of GenerationEvents.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@mad/schema": "*"
  },
  "devDependencies": {
    "typescript": "~5.9.0",
    "vitest": "^4.0.8"
  }
}
